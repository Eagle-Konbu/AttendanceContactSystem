<%= form_with(model: line_reply_message, local: true) do |form| %>
  <% if line_reply_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_reply_message.errors.count, "error") %> prohibited this line_reply_message from being saved:</h2>

      <ul>
      <% line_reply_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_field :detail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
