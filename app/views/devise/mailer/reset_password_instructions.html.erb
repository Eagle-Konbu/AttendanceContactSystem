<p>Hello <%= @resource.email %>!</p>

<p>誰かがパスワードを変更しようとしています。パスワードの変更は以下のリンクから可能です。</p>

<p><%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>このメールに心当たりのない場合は、このメールは無視してください。</p>
<p>上記リンクで設定されるまで、あなたのパスワードが変更されることはありません。</p>
