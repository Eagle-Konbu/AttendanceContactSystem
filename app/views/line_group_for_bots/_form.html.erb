<%= form_with(model: line_group_for_bot, local: true) do |form| %>
  <% if line_group_for_bot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_group_for_bot.errors.count, "error") %> prohibited this line_group_for_bot from being saved:</h2>

      <ul>
      <% line_group_for_bot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.text_field :group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
